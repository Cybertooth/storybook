{
    "name": "storybook-monorepo",
    "private": true,
    "workspaces": [
        "web-ui",
        "backend",
        "api"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=@storybook/web-ui",
        "build": "npm run build --workspace=@storybook/web-ui"
    },
    "devDependencies": {
        "@prisma/client": "^7.4.1"
    }
}
