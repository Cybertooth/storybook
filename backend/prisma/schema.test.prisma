generator client {
  provider   = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "sqlite"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  stories             Story[]
  characters          Character[]
  relationships       Relationship[]
  locations           Location[]
  events              PlotEvent[]
  chapters            Chapter[]
  notes               Note[]
  unresolvedQuestions UnresolvedQuestion[]
  appSettings         AppSettings?
}

model Story {
  id           String   @id @default(uuid())
  userId       String
  title        String
  summary      String
  theme        String?
  coreQuestion String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  user                User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
  characters          Character[]
  relationships       Relationship[]
  locations           Location[]
  events              PlotEvent[]
  chapters            Chapter[]
  notes               Note[]
  unresolvedQuestions UnresolvedQuestion[]
}

model Character {
  id          String   @id @default(uuid())
  userId      String
  storyId     String
  name        String
  role        String
  description String
  traits      String
  arcLie      String?
  arcTruth    String?
  arcGhost    String?
  avatarUrl   String?

  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  story Story @relation(fields: [storyId], references: [id], onDelete: Cascade)

  events PlotEvent[] @relation("CharacterEvents")
}

model Relationship {
  id          String @id @default(uuid())
  userId      String
  storyId     String
  sourceId    String
  targetId    String
  type        String
  description String

  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  story Story @relation(fields: [storyId], references: [id], onDelete: Cascade)
}

model Location {
  id           String  @id @default(uuid())
  userId       String
  storyId      String
  name         String
  description  String
  sensorySight String?
  sensorySound String?
  sensorySmell String?
  sensoryTouch String?
  sensoryTaste String?

  user   User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  story  Story       @relation(fields: [storyId], references: [id], onDelete: Cascade)
  events PlotEvent[]
}

model PlotEvent {
  id             String  @id @default(uuid())
  userId         String
  storyId        String
  title          String
  description    String
  order          Int
  chapterId      String?
  locationId     String?
  status         String?
  plotThread     String?
  emotionalValue Int?

  user       User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  story      Story       @relation(fields: [storyId], references: [id], onDelete: Cascade)
  chapter    Chapter?    @relation(fields: [chapterId], references: [id], onDelete: SetNull)
  location   Location?   @relation(fields: [locationId], references: [id], onDelete: SetNull)
  characters Character[] @relation("CharacterEvents")
}

model Chapter {
  id      String @id @default(uuid())
  userId  String
  storyId String
  title   String
  content String
  order   Int
  status  String

  user   User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  story  Story       @relation(fields: [storyId], references: [id], onDelete: Cascade)
  events PlotEvent[]
}

model Note {
  id        String   @id @default(uuid())
  userId    String
  storyId   String
  content   String
  createdAt DateTime @default(now())

  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  story Story @relation(fields: [storyId], references: [id], onDelete: Cascade)
}

model UnresolvedQuestion {
  id         String   @id @default(uuid())
  userId     String
  storyId    String
  question   String
  details    String
  isResolved Boolean  @default(false)
  answer     String?
  createdAt  DateTime @default(now())

  user  User  @relation(fields: [userId], references: [id], onDelete: Cascade)
  story Story @relation(fields: [storyId], references: [id], onDelete: Cascade)
}

model AppSettings {
  id          String  @id @default(uuid())
  userId      String  @unique
  llmProvider String  @default("gemini")
  geminiKey   String?
  geminiModel String?
  openaiKey   String?
  theme       String  @default("system")

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
}
